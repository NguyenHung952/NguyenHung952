version: '3.9'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - JWT_SECRET=change-me
  nginx:
    image: nginx:stable
    ports:
      - "80:80"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api
